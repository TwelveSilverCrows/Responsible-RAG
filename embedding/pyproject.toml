[project]
name = "openvino-embedder"
version = "0.1.0"
description = "OpenVINO-powered embedding server using BGE models"
requires-python = ">=3.10"
dependencies = [
    "fastapi==0.111.0",
    "uvicorn==0.30.1",
    "optimum-intel[openvino]>=1.27,<2",
    "transformers>=4.41,<5",
    "torch>=2.4,<3",
    "numpy>=1.26,<2",
]
