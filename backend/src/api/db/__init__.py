"""
db/__init__.py — MongoDB data access layer
============================================

Minimal MongoDB helpers using raw ``pymongo`` — no ODM overhead.
All collections are accessed via helper functions in ``database.py``.
"""
